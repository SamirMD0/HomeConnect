import React,{useState} from 'react';
import toast from 'react-hot-toast';
import { Modal } from '../../../components/ui/Modal';
import { businessLabels } from '../../../shared/labels/business-labels';
import { normalizeFinancialError } from '../../customer-financial/utils/financial-form-errors';
export const SupplierActionDialog:React.FC<{open:boolean;title:string;confirmLabel:string;onClose:()=>void;onConfirm:(input:{reason:string;accountPassword:string})=>Promise<unknown>}>=({open,title,confirmLabel,onClose,onConfirm})=>{const[reason,setReason]=useState('');const[password,setPassword]=useState('');const[pending,setPending]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setPending(true);try{await onConfirm({reason,accountPassword:password});setReason('');setPassword('');onClose();}catch(error){toast.error(normalizeFinancialError(error).message);}finally{setPending(false);}};return <Modal isOpen={open} onClose={onClose} title={title}><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-semibold">{businessLabels.supplier.reason}<textarea required minLength={5} dir="auto" value={reason} onChange={e=>setReason(e.target.value)} className="user-text-input mt-1 w-full rounded-md border border-slate-300 p-3"/></label><label className="block text-sm font-semibold">{businessLabels.supplier.accountPassword}<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-md border border-slate-300 px-3 py-2"/></label><button disabled={pending} className="w-full rounded-md bg-red-600 px-4 py-2.5 font-semibold text-white disabled:opacity-50">{pending?'Working... / جارٍ التنفيذ':confirmLabel}</button></form></Modal>};
